fn main() {
    // With UniFFI proc-macros (uniffi::setup_scaffolding!() in android.rs),
    // we don't need UDL-based scaffolding generation.
    // The scaffolding is generated at compile time from the Rust code.
}
